import PrimeNumberTheoremAnd.ArtinLSeriesNatConvolutionSum

/-!
## Artin L-series: direct sums on coefficients

This file is a convenience re-export for the coefficient-level statements about direct sums:

- `ArtinLSeriesNatPrimePowSum`: prime-power identity,
- `ArtinLSeriesNatConvolutionSum`: full Dirichlet convolution identity
  `coeffNat (ρ₁ ⊕ ρ₂) = coeffNat ρ₁ ⍟ coeffNat ρ₂`.

No analytic statement about multiplying the global `LSeries` is asserted here.
-/
